#include<stdio.h>
/*test()
{
	int i = 1;
	i++;
	printf("i=%d\n", i);


}
int main(void)
{
	int a;
	printf("输入一个数:\n");
	scanf_s("%d", &a);
	if (1== a % 2)
		printf("%d是一个奇数", a);
	else
		printf("%d不是一个奇数", a);
	return 0;
	}*/
	


int main(void)
{
	int ch=0;
	while ((ch = getchar()) != EOF)
	{
		if (ch < '0' || ch>'9')//为什么敲出任何数字都是 可行的呢,然后在学一学ascii码, 有点晕
		
			continue;
		else
			putchar(ch);

	}
	return 0;
}
